name: Manualtrigger2

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Who to greet"
  schedule:
    - cron: '30 6 * * 4'      
        

jobs:
    hello:
        runs-on: windows-latest

        steps:
        - name: Echo Hello
          run:  java -version
